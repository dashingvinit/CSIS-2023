import React from 'react';
import { favicon } from '../assets';

const CSISlogo = () => {
  return (
    <div className="flex justify-top mb-20 ">
      <img className="justify-center" src={favicon} alt="" />
    </div>
  );
};

export default CSISlogo;
